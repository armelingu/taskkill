{
  "name": "taskkill-dev",
  "private": true,
  "description": "Ferramentas de dev/teste do Taskkill (o app em si não tem build step).",
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/js/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
