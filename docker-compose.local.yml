# Overlay de desenvolvimento local: publica a porta e afrouxa o cookie (HTTP).
# Integrações externas são configuradas pela própria UI (não há serviço extra).
services:
  app:
    ports:
      - "5091:8000"
    environment:
      TASKKILL_COOKIE_SECURE: "0"

